//run this cmd with adminstrator mode
npm install -g typescript nodemon ts-node prettier

npm install
npm start

//to create and modify db set (like adding new table, etc)
npx prisma migrate dev --name init